<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_movie_item"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.codepath.aaneja.flix.models.MovieItem">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:id="@+id/tvTitle"
        android:gravity="center"
        android:text="A long movie title "
        android:textColor="@color/colorTitle"
        android:textSize="28sp"
        android:layout_marginBottom="3sp" />

    <ImageView
        android:layout_width="300dp"
        android:layout_height="@dimen/imageBackdropHeight"
        app:srcCompat="@mipmap/ic_launcher"
        android:layout_below="@+id/tvTitle"
        android:layout_alignParentStart="true"
        android:id="@+id/ivMovie" />

    <TextView
        android:text="A rather long movie overview"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/imageBackdropHeight"
        android:layout_below="@id/tvTitle"
        android:id="@+id/tvOverview"
        android:layout_alignParentEnd="true"
        android:scrollHorizontally="true"
        android:ellipsize="end"
        android:maxLines="8"
        android:layout_toEndOf="@+id/ivMovie"
        android:paddingLeft="10dp"
        android:textSize="18sp" />


</RelativeLayout>
